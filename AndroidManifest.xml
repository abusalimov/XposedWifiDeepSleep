<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.abusalimov.xposed.wifideepsleep"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <meta-data android:value="true" android:name="xposedmodule"/>
        <meta-data android:value="2.0*" android:name="xposedminversion"/>
        <meta-data android:value="Prevents Wi-Fi from waking up on the lock screen or by application Wifi locks." android:name="xposeddescription"/>
    </application>

</manifest>
